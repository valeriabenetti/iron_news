<div class="jumbotron landing-jumbotron">
  <h1>In the News!</h1>
  <p>News about what matters the most to those in Tech</p>
</div>

<div class="panel panel-default">
  <table class="table-responsive table table-striped table-bordered table-hover">
    <thead>
      <tr>
        <th colspan="1">#</th>
        <th colspan ="6">Title</th>
        <th colspan="4">Created At</th>
        <th colspan="3">Email</th>
      </tr>
    </thead>
    <tbody class="table-body">
      <%- @stories.each.with_index do |story, index| %>
      <tr>
        <td><%= index + 1 %> . </td>
        <td colspan="6"><a href="<%= story.url %>"><%= story.title %></a></td>
        <td colspan="4">Posted at <%= localize(story.created_at, :format => :long) %></td>
        <td colspan="2"><%= story.email %></td>
        <!-- <td>
          <%= link_to story do %>
            <span class="glyphicon glyphicon-ok" aria-hidden="true"></span>
          <% end %>
        </td>
        <td>
          <%= link_to edit_story_path(story) do %>
            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
          <% end %>
        </td>
        <td>
          <%= link_to story, method: :delete, data: { confirm: 'Are you sure?' } do %>
            <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
          <% end %>
        </td> -->
      </tr>
      <% end %>
    </tbody>
  </table>

</div>
